# Мегапромпт: редизайн вкладки «Контроли» под скин Glass Dark

РЕПОЗИТОРИЙ: https://github.com/sledovatel61/porayonka.git
БАЗА: ветка `main` (или закреплённая сессионная ветка — тогда от неё).
ПРИЛОЖЕНИЕ: `porayonka-app/main.py`
ЗАПУСК: `python porayonka-app/main.py`
FLET: строго `0.23.2` (не повышать).

---

## 1. Контекст

Десктопное приложение «Порайонка» (Python + Flet 0.23.2, Windows, тёмная тема).
Третья вкладка «Контроли» **уже полностью работает** (список, фильтры, карточка,
пункты задания, архив, вложения, импорт/экспорт Excel, сетевой режим, уведомления).
Функционал трогать НЕ нужно.

Проблема — только визуал и восприятие. Жалобы пользователя после живого теста:

1. «Всё неброско, всё сливается, текст трудно воспринимается» — низкий контраст,
   плоские серые панели, мелкие кегли.
2. «Фильтры наверху в куче друг на друге» — плохо читаются, значения обрезаны.
3. «Заголовки входящих номеров обрезаются» — колонка «вх. №» слишком узкая,
   `Иссоп-216-2083-25` не влезает.
4. «Календарь не на русском, дата выбирается только после нажатия OK» —
   стандартный `ft.DatePicker` не устраивает.

Выбран целевой дизайн: **мокап `01 Glass Dark`** (победитель отбора из 10 вариантов).

---

## 2. Референсы (обязательно изучить перед стартом)

- **`design/mockups_portable/photo/01_glass_dark.png`** — фото-эталон 1600×900.
  Открыть и изучить: шапка с кнопками, два ряда фильтров, чипы-счётчики, таблица
  со статусными полосами, стеклянная карточка контроля поверх с русским календарём
  «Август 2026» (Пн Вт Ср Чт Пт Сб Вс).
- **`design/mockups_portable/photo/README.md`** — раздел «01 · Glass Dark» —
  стилевая карточка (источник истины по цветам; пиксели картинки — нет).

### Палитра Glass Dark (внедрить)

| Роль | Hex |
|---|---|
| Фон вкладки | `#0a1024` |
| Поверхность «стекло» | `#16213dcc` (alpha CC ≈ 80%) |
| Границы | `#ffffff1a` (белый 10%) |
| Светлая кромка сверху панелей | `#ffffff2e` |
| Текст | `#f2f5ff` |
| Вторичный текст | `#93a3c7` |
| Приглушённый | `#5d6b8f` |
| Акцент (кнопки, активное) | `#4f8cff` |
| Просрочено | `#ff5c6e` |
| Сегодня | `#ffd166` |
| Скоро | `#ff9f43` |
| В работе | `#2fd08b` |
| Исполнено | `#8a94ad` |
| Завершён | `#4f8cff` |

Радиусы 10–14 px. Шрифт Segoe UI (системный). Плотность средняя.

### Как делается «стекло» в Flet 0.23.2 (важно, ограничения движка)

- Полупрозрачный `bgcolor="#16213dcc"` — разрешено и работает.
- Светлая кромка: `border=ft.border.only(top=ft.BorderSide(1, "#ffffff2e"),
  left=..., right=..., bottom=...)` — НО: у Container с `border_radius`
  рамка обязана быть **равномерной по толщине** на всех сторонах
  (см. AGENTS.md §26: `border.only` с разной толщиной ломает скругление).
  Поэтому: все стороны 1 px, верхняя чуть светлее по ЦВЕТУ, не по толщине.
- Свечение статусов: заливка `<color>22` + рамка 1 px `<color>` — без теней.
- **Запрещено**: тени/`BoxShadow`, `gradient` на мелких контейнерах,
  `backdrop-filter` (в Flet нет), `animate*` внутри скролл-колонки.
  Эффект «стекла» достигается ТОЛЬКО полупрозрачным фоном + границами.

---

## 3. Объём работ — ТОЛЬКО вкладка «Контроли»

Файлы, которые меняются:

- `porayonka-app/ui/controls/controls_tab.py` — основной рестайл.
- `porayonka-app/ui/controls/control_card_modal.py` — если используется для
  превью/совместимости, привести к той же теме (основной таб использует свой
  overlay — он в приоритете).
- **НОВЫЙ** `porayonka-app/ui/controls/glass_theme.py` — локальная палитра
  `GLASS = {...}` + хелперы: `glass_panel(...)`, `status_pill(...)`,
  `glass_button(...)`, `ghost_button(...)`, `chip(...)`. Вся тема — здесь,
  чтобы не рассыпать hex по коду.
- **НОВЫЙ** `porayonka-app/ui/controls/russian_calendar.py` — кастомный
  календарь (см. §5).

НЕ менять: `core/controls_models.py`, `core/controls_data.py`,
`core/controls_exporter.py`, `core/constants.py` (глобальная палитра приложения),
`main.py`, вкладки «Следственные отделы» и «Зональные», любые контракты функций
(`create_controls_tab`, обработчики, сетевой polling, экспорт/импорт, архив,
вложения, роли admin/user). Логика фильтрации/сортировки сохраняется как есть —
меняется только её визуальная подача и расположение контролов.

---

## 4. Требования к редизайну (по мокапу)

### 4.1 Общая композиция (как на мокапе, сверху вниз)

1. **Шапка вкладки**: слева «Контроли» (size 20, bold, `#f2f5ff`) + точка статуса
   сети + «Локально»/«Сеть: роль»; справа кнопки:
   - «Добавить контроль» — акцентная `#4f8cff`, белый текст, height 40,
     radius 10, иконка ADD. Главная кнопка — самая заметная на экране.
   - «Импорт Excel», «Как в таблице», «Настройки» — ghost-стекло
     (`bgcolor="#16213dcc"`, рамка `#ffffff1a`, текст `#f2f5ff`).
   - «Экспорт Excel» — зелёная `#2fd08b` (тёмный текст `#04121f`).
   Шапка — стеклянная панель (radius 12, кромка сверху).
2. **Фильтры — два ровных ряда** (как на мокапе):
   - Ряд 1: поиск (grow), «Все статусы», «Все типы».
   - Ряд 2: «Все инициаторы», «Все исполнители», «Все контролёры», «С:», «По:»,
     «Сброс» справа.
   - Каждый фильтр — стеклянный Dropdown/TextField: height 40, radius 10,
     `hint_text` (НЕ `label` — AGENTS.md §31.5), `text_style` size 13,
     `hint_style` `#5d6b8f`, bgcolor `#0d1830`-подобный вдавленный тон,
     рамка `#ffffff1a`. Значения должны читаться ПОЛНОСТЬЮ — ширины подобрать
     так, чтобы «Все контролёры» / «Потемкин С.А.» не обрезались.
   - Кнопки дат «С:»/«По:» открывают **кастомный русский календарь** (§5),
     НЕ `ft.DatePicker`.
3. **Чипы-счётчики**: Все 134 · Просрочено 2 · Сегодня 2 · Скоро 5 · В работе 114 ·
   Исполнено 9. Формат: цветная точка 8 px + подпись size 12 + число bold.
   Активный чип — заливка `<status_color>22` + рамка 1 px `<status_color>`;
   «Все» — акцент `#4f8cff`. Height 30, radius 15 (пилюля). Кликабельны
   (фильтрация по статусу) — логика уже есть, сохранить.
   Переключатель «Активные | Архив» — справа в этом же ряду (сегмент-контрол,
   как сейчас, но в стекле).
4. **Таблица**:
   - Шапка колонок: height 34, фон `#0d1830`, текст `#93a3c7`, size 11, bold,
     ВЕРХНИЙ РЕГИСТР, сортировка стрелками сохраняется.
   - Строки: height 54, size 13 (было 12 — мелко!), цвет `#f2f5ff`.
     Чередование фона: `#16213d66` / `transparent`. Hover — `#ffffff08`
     (через `on_hover` + `update()`, без animate).
   - Левая статусная полоса: 4 px, radius 2, цвет статуса — сохранить.
   - **Колонка «вх. №» — расширить до ≥150 px**, значение size 12.5–13 bold,
     БЕЗ обрезания (`Иссоп-216-1017-26/дсп` должно помещаться; пересчитать
     `_layout_widths`: ужать «Содержание» и «Исполнители», а не номер).
   - Статус — пилюля: заливка `<color>22`, рамка 1 px `<color>`, иконка 12,
     текст size 11 bold цвета статуса. Текущие `STATUS_LABELS`/`STATUS_ICONS`
     сохранить, перекрасить в палитру §2.
   - Кнопки действий (карандаш/архив): 30×30, ghost, hover `#ffffff12`.
   - Дата «Срок исполн.» — bold, для OVERDUE/TODAY/SOON — цветом статуса.
5. **Фон вкладки** — `#0a1024` (обернуть контент вкладки в Container с этим
   bgcolor; глобальный `page.bgcolor` НЕ трогать).

### 4.2 Карточка контроля (detail overlay)

Уже реализована как Container-overlay в `ft.Stack` (master-detail, БЕЗ вложенных
диалогов — AGENTS.md §31, ломать это запрещено). Рестайл:

- Панель: width ~860, max height ~740, radius 14, стекло `#16213dcc`,
  кромка сверху `#ffffff2e`. Фон-затемнение за панелью `#04070fcc`.
- Заголовок «Карточка контроля» size 17 bold + крестик закрытия.
- Поля: label size 11–12 `#93a3c7` СВЕРХУ поля; поля ввода вдавленные
  (`bgcolor="#0d1830"`, рамка `#ffffff1a`, radius 10, height 40, size 13).
  Многострочное «Содержание» — min 3 строки.
- Исполнители/ответственные — существующий ИНЛАЙН-мультивыбор с чекбоксами
  (НЕ диалог!): header-строка «Исполнители · Выбрано: N» со стрелкой,
  раскрывающаяся стеклянная панель со списком. Сохранить логику, рестайлить.
- Пункты задания — строки-карточки в стекле: чекбокс, название, ответственные
  (inline-пикер), дата (кастомный календарь), комментарий, удалить.
- Кнопки внизу: «Сохранить» — акцент `#4f8cff`; «Отмена» — ghost.
- Все confirm-диалоги (удаление, архив) — `page.open()`/`page.close()`,
  `AlertDialog` с `bgcolor="#16213d"`, radius 14.

---

## 5. Кастомный русский календарь (ключевая фича)

**НОВЫЙ файл `ui/controls/russian_calendar.py`.** Заменяет `ft.DatePicker`
во ВСЕЙ вкладке (карточка, пункты задания, фильтры «С/По»). Стандартный
DatePicker английский и требует OK — не подходит.

ТЗ:

- API: `create_russian_date_field(page, value: Optional[str], on_change,
  hint="Дата") -> ft.Control` — поле ввода (read_only текст `dd.mm.yyyy` +
  иконка календаря); клик открывает панель календаря.
- Панель — **инлайн** (выпадашка под полем через `visible` toggle, либо
  мини-overlay внутри той же карточки/ряда — НЕ `AlertDialog`, не второй слой
  overlay поверх detail_overlay, см. §31 AGENTS.md про вложенность).
- Структура панели (стекло, radius 12, padding 10, width ~280):
  - Шапка: стрелка ‹ (CHEVRON_LEFT), «Август 2026» (bold, size 13,
    capitalize), стрелка › (CHEVRON_RIGHT). Листание по месяцам.
  - Дни недели: Пн Вт Ср Чт Пт Сб Вс (size 10, `#5d6b8f`, bold).
  - Сетка 6 рядов × 7 ячеек, ячейка `Container(width=34, height=32,
    border_radius=8)`: число size 12; дни соседних месяцев — `#5d6b8f`
    приглушённые; hover — `#ffffff12` (on_hover+update);
    **сегодня** — рамка 1 px `#4f8cff`; **выбранный** — заливка `#4f8cff`,
    белый текст.
  - **Клик по дню = мгновенный выбор**: вызвать `on_change("YYYY-MM-DD")`,
    закрыть панель, обновить текст поля. Никаких OK/Cancel.
  - Кнопки снизу: «Сегодня» (ghost, size 11) и «Очистить» (ghost, ставит None).
- Русские названия месяцев: Январь…Декабрь (именительный падеж в шапке).
  Неделя с понедельника. Всё через stdlib `calendar`/`datetime`, без зависимостей.
- Никаких `animate`, `shadow`, `gradient`, `wrap` — фиксированные Row'ы.
- DatePicker'ы из overlay удалить (сейчас они там копятся — БАГ F из AGENTS.md §31).

---

## 6. Типографика и читаемость (сводно)

| Элемент | Было | Стало |
|---|---|---|
| Строка таблицы | size 12, height 46 | size 13, height 54 |
| Заголовки колонок | size 11 | size 11, bold, UPPERCASE, `#93a3c7` |
| Статус-пилюля | size 10 | size 11 bold, заливка+рамка |
| Фильтры | size 11–12, обрезаны | size 13, hint `#5d6b8f`, полные значения |
| Заголовок вкладки | — | size 20 bold |
| Карточка: labels | — | size 11–12 сверху поля |
| Карточка: поля | — | size 13, height 40 |

Контраст текста к фону везде ≥ 4.5:1 (палитра §2 это даёт).

---

## 7. Жёсткие технические ограничения (AGENTS.md §15.11, 22, 24.3, 26, 30.6, 31.5)

Внутри `Column(scroll=ft.ScrollMode.AUTO)` вкладки ЗАПРЕЩЕНО:

- `expand=True` (кроме горизонтальных спейсеров внутри обычных Row);
- `wrap=True` в Row; `animate`/`animate_opacity`/`animate_scale` на Container;
- `gradient` на мелких контейнерах (шапка вкладки — допустимо, но в этом
  скине градиент НЕ нужен: только полупрозрачность);
- `shadow`/`BoxShadow` на элементах внутри скролла;
- `elevation={"":N,"hovered":M}` и `overlay_color` в ButtonStyle;
- `scroll=HIDDEN` в Row, где есть `tight=True` + `expand` (БАГ 30.1);
- `page.overlay.append(picker)` без `page.update()` (БАГ A §31);
- вложенные `AlertDialog` поверх карточки (БАГ B §31) — только инлайн-панели;
- `header_row.controls = [...]` у Container — только `.content` (БАГ C §31);
- `label=` у filter Dropdown — только `hint_text` + `dense` (БАГ E §31);
- `border.only` с РАЗНОЙ толщиной сторон на Container с `border_radius`
  (§26 — ломает скругление на Windows).

Прочее: `ft.icons.*` (не emoji), `print()` в консоль только ASCII, UI-текст —
на русском, Flet строго 0.23.2, новых зависимостей не добавлять.

---

## 8. Проверка (обязательно перед коммитом)

```bash
python -c "import sys; sys.path.insert(0, 'porayonka-app'); import main"
```

Плюс headless-smoke (page-заглушка): вкладка строится, фильтры переключаются,
карточка открывается, календарь листается и выбирает дату одним кликом,
экспорт/импорт/архив работают как раньше.

Ручная приёмка в GUI (описать в финальном сообщении чек-листом):
1. Вкладка выглядит как мокап 01: стеклянные панели, читаемый текст, статусы яркие.
2. «вх. №» не обрезан. 3. Календарь русский, дата ставится одним кликом без OK.
4. Фильтры читаются полностью. 5. Карточка/пункты/архив/Excel/сеть — без регрессий.

---

## 9. Финиш

Когда закончишь — коммить только в отдельную ветку от main (не в main; если
сессия закреплена на ветке — используй её), и в последнем сообщении напиши мне
точное имя этой ветки.
