# PROMPT — Доработка вкладки «Контроли», раунд 22 (инструкция пользователя, 11.08.2026)

Скрины и лог: `design/screenshots/11.08.2026/` (коммит `701c16b` в `main`,
13 новых файлов раунда 22 поверх раунда 21).

## Задачи (дословно)

1. «Добавил новую карточку (скрин). у двух исполнителей срок 31.08.2026 по
   п. 1 и 3, а вот п. 10 срок поставил 28.08.2026. Далее я выбрал, что п. 10
   к сроку 28.08.2026 исполнен, по идее, срок должен был сдвинуться к
   следующему, то есть к 31.08.2026, т.к. предыдущий исполнен. Возможно это
   связано, что в левой колонке я сроки не ставил, а проставил только в
   пунктах, но по идее так быть не должно.»
2. «удалил все контроли и заново импортировал из таблицы (сделал скрин),
   подгрузились как надо пункты в контролях, но все с просрочками — тут все
   понятно, как исправим, так поправлю в таблице и заново импортирую, но я
   попытался сейчас отметить исполнение пунктов вручную, но кнопка сохранить
   не нажимается… я снова открыл эту карточку и уже нажал кнопку "исполнил
   пункт", начал выбирать по одному пункту (а было бы хорошо, если было бы
   можно отмечать сразу несколько пунктов сразу), и вот тогда на главном
   экране пункты стали отмеченные как исполнены, но при этом все равно нажать
   кнопку сохранить нельзя, только закрывать ее. также дата следующего
   исполнения перенеслась на 01.09.2026, как и надо, а срок разового контроля
   остался на 16.02.2026.»
3. «когда прикрепляешь фото, то, что прикрепилось не отображается, далее я
   жму сохранить, после этого строка с контролем задваивается и их становится
   2 одинаковых. далее снова открываю карточку и вижу, что файл прикрепился,
   но без предпросмотра. Нажимаю лупу предпросмотра и пишет ошибку. Я сделал
   скрин.» (на скрине — toast «Файл вложения не найден»)
4. «Опять проблема с исполнителями, там полная неразбериха, в фильтре
   исполнителей не все есть, кто есть в справочнике, зато они перенеслись в
   прочие почему-то.»
5. «Сохранил лог, там есть ошибки.»

## Лог (design/screenshots/11.08.2026/ЛОГ.txt)

```
Traceback (most recent call last):
  File "...\porayonka-app\ui\controls\controls_tab.py", line 2634, in _confirm
  File "...\porayonka-app\core\controls_data.py", line 820, in delete_attachment
  File "...\porayonka-app\core\controls_data.py", line 731, in get_attachment_source_path
  File "...\porayonka-app\core\controls_data.py", line 68, in get_attachment_dir
TypeError: unsupported operand type(s) for /: 'WindowsPath' and 'NoneType'
```

(pythonw, повторяется; путь пользователя:
`C:\Users\User\Documents\Исходная программа\porayonka-app v2 DARK final\porayonka-app`)
