# Зависимости тестов и стендовых инструментов W01 (не входят в runtime-контур)
-r requirements.txt
pytest==9.1.1
httpx==0.28.1
# reportlab нужен только генератору синтетического PDF с кириллицей (tools/make_sample_pdf.py)
reportlab==5.0.1
# pypdf — только для автоматической проверки кириллицы внутри синтетического PDF
pypdf==6.1.1
